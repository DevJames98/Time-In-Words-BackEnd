server:
  port: 2020

spring:
  application:
    name: time
  datasource:
    driver-class-name: org.postgresql.Driver
    initialization-mode: never
    username: devjames98
    password: n00b3rl1f3
    url: jdbc:postgresql://javareact2002.crtdildu21ho.us-east-2.rds.amazonaws.com:5432/postgres
    # url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        default-schema: ${DB_SCHEMA}
    show-sql: true 
    
    
